/**
 * M0 scaffold shell. Actual layout (ToolBar, CanvasViewport, right panel stack, timeline)
 * lands in M1-M3. For now this just proves the toolchain runs end-to-end.
 */
export function App(): JSX.Element {
  return (
    <main className="app-shell">
      <h1>RaBIT</h1>
      <p>Scaffold running. Full UI arrives in M1-M3.</p>
    </main>
  );
}
